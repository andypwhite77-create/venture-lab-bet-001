colony.cobaltindustrial.tech {
    encode zstd gzip
    @allowed path /dashboard /colony/live
    handle @allowed {
        reverse_proxy app:8000
    }
    handle {
        respond "Not Found" 404
    }
}
